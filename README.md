# Calorie Finder - Поиск калорийности продуктов

Приложение для поиска информации о калорийности и пищевой ценности продуктов через Open Food Facts API.

## Функциональность

### 🔍 Поиск продуктов
- **Поиск по штрихкоду** - точный поиск по EAN/штрихкоду продукта
- **Поиск по названию** - текстовый поиск продуктов по наименованию

### 📊 Отображение информации
- **Таблица результатов** с основными параметрами продуктов
- **Детальная информация** при выборе продукта из таблицы
- **Пищевая ценность** на 100г продукта:
  - Калории (ккал)
  - Белки (г)
  - Жиры (г) 
  - Углеводы (г)
- **Дополнительная информация**:
  - Бренд производителя
  - Размер упаковки
  - Размер порции

## Использованные технологии и библиотеки

### 🐍 Python библиотеки
- **`requests`** - для HTTP-запросов к API Open Food Facts
- **`PyQt6`** - для создания графического интерфейса

### 🔧 Основные функции приложения

#### Функции работы с API
```python
def get_product_by_barcode(barcode: str, fields=None, lang="ru", country="ru") -> dict
```
- Получение данных о продукте по штрихкоду
- Параметры: штрихкод, поля для запроса, язык, страна
- Возвращает: словарь с данными продукта

```python
def search_products(query: str, page_size=5, fields=None, lang="ru", country="ru") -> dict
```
- Поиск продуктов по текстовому запросу
- Параметры: поисковый запрос, размер страницы, поля, язык, страна
- Возвращает: словарь с результатами поиска

```python
def extract_kcal(nutriments: dict) -> dict
```
- Извлечение информации о калорийности и БЖУ из данных продукта
- Возвращает значения на 100г и на порцию

#### Классы PyQt6

**`SearchWorker(QThread)`** - класс для многопоточных запросов
- `finished` - сигнал успешного завершения поиска
- `error` - сигнал ошибки при поиске
- `run()` - метод выполнения запроса в отдельном потоке

**`MainWindow(QMainWindow)`** - главное окно приложения
- `setup_barcode_tab()` - настройка вкладки поиска по штрихкоду
- `setup_search_tab()` - настройка вкладки поиска по названию
- `setup_results_area()` - настройка области результатов
- `search_by_barcode()` - обработчик поиска по штрихкоду
- `search_by_name()` - обработчик поиска по названию
- `display_results()` - отображение результатов поиска
- `show_product_details()` - показ детальной информации о продукте

### 🎯 Виджеты PyQt6

#### Контейнеры и компоновки
- **`QMainWindow`** - главное окно приложения
- **`QWidget`** - базовый виджет
- **`QTabWidget`** - виджет вкладок
- **`QVBoxLayout`**, **`QHBoxLayout`** - вертикальная и горизонтальная компоновки
- **`QFormLayout`** - компоновка для форм
- **`QGroupBox`** - группа элементов с рамкой

#### Элементы управления
- **`QLineEdit`** - поле ввода текста
- **`QPushButton`** - кнопка
- **`QTableWidget`** - таблица для отображения данных
- **`QTextEdit`** - многострочное текстовое поле
- **`QLabel`** - метка текста
- **`QProgressBar`** - индикатор прогресса

#### Диалоговые окна
- **`QMessageBox`** - окно сообщений (предупреждения, ошибки, информация)

### 🔄 Система PyQt6

#### Сигналы и слоты
- **`pyqtSignal`** - пользовательские сигналы
- **`connect()`** - соединение сигналов со слотами
- **`returnPressed`** - сигнал нажатия Enter в поле ввода
- **`clicked`** - сигнал клика по кнопке
- **`cellClicked`** - сигнал клика по ячейке таблицы

#### Многопоточность
- **`QThread`** - класс для работы с потоками
- Асинхронные HTTP-запросы без блокировки GUI

#### Обработка событий
- **`QApplication`** - главный класс приложения
- **`exec()`** - запуск главного цикла событий

## Установка и запуск

### Требования
- Python 3.7+
- Установленные зависимости

### Установка зависимостей
```bash
pip install pyqt6 requests
```

### Запуск приложения
```bash
python main.py
```

## Особенности реализации

- **Многопоточность** - запросы к API выполняются в фоновом режиме
- **Обработка ошибок** - корректная обработка сетевых ошибок и ошибок API
- **Локализация** - поддержка русского языка интерфейса и данных
- **Адаптивный интерфейс** - автоматическое масштабирование элементов
- **Кэширование** - временное хранение результатов запросов

## API Open Food Facts

Приложение использует официальное API Open Food Facts:
- База данных: https://world.openfoodfacts.org/
- API v2 для поиска по штрихкоду
- Search API v2 для текстового поиска
- Поддержка русского языка (lc=ru, cc=ru)
